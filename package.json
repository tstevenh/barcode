{
  "name": "barcode-vercel-fast",
  "version": "1.0.0",
  "private": true,
  "description": "Fast Vercel deploy for barcode web + API.",
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "bwip-js": "4.11.1"
  },
  "scripts": {
    "build": "node scripts/build-pages.mjs",
    "start": "node scripts/build-pages.mjs && node server.js",
    "dev": "node scripts/build-pages.mjs && node server.js",
    "verify:decode": "node scripts/verify-decode.mjs"
  },
  "devDependencies": {
    "pngjs": "^7.0.0",
    "zxing-wasm": "^3.1.0"
  }
}
